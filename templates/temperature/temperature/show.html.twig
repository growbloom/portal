{% extends 'base.html.twig' %}

{% block title %}Temperature{% endblock %}

{% block body %}
    <h1>Temperature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temperature.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ temperature.value }}</td>
            </tr>
            <tr>
                <th>Measured_at</th>
                <td>{{ temperature.measuredAt ? temperature.measuredAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('temperature_temperature_index') }}">back to list</a>

    <a href="{{ path('temperature_temperature_edit', {'id': temperature.id}) }}">edit</a>

    {{ include('temperature/temperature/_delete_form.html.twig') }}
{% endblock %}
